const button = document.getElementById("btn-el");
const paragraf = document.getElementById("paragraf");

button.addEventListener("click", function () {
  if ((paragraf.innerText = true)) {
    paragraf.innerText = "おめでとう！^^";
  }
});
